apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: paralelas-project
spec:
  type: NodePort
  selector:
    app: frontend   # coincide con la etiqueta del pod en el Deployment
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
